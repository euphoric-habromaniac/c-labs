/* Write a program in C to determine whether a person is eligible to vote or not based on age. */

#include <stdio.h>
int main(){int age; scanf("%d",&age); if(age>=18) printf("Eligible\n"); else printf("Not Eligible\n"); return 0;}